1. Open command prompt. Navigate to this directory (use cd & dir commands).
2. Type Python -m http.server to host files to server locally.
3. View html code in local server 'http://localhost:8000/'
